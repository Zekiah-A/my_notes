# My notes
A simple python notetaking application made for an assignment.

Run with `python3 main.py`. Depends on `tkinter` and `sqlite3`.

Debug the database with `sqlite3` command with `.open notes.db`.